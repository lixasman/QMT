# 2026-03-11 股票筹码与微观因子每日简报

数据源：`output/integration/stock_chip/stock_batch_results_20260311.csv`

## 状态确认
- 8/8 标的均为延续状态（非冷启动）：`prev_trade_date=20260310`、`prev_state_loaded=True`、`cold_start_used=False`。

## 组合层面结论
- `profit_ratio` 组合均值继续上行，但 `ofi_daily_z` 均值回落，呈现“盈利筹码扩大但净买盘不如昨日”的特征。
- 波动与冲击较昨日小幅回升，短线更偏震荡分化。

## 明日操作要点（T+1，仅对已有底仓做T）
### 可回踩小仓做T
- `601988.SH`：`OFI_z` 与 `VWAP_z` 双转正，支撑位 `5.35`。靠近支撑回补，冲高减仓，不追高。
- `000625.SZ`：结构修复，支撑 `10.85`、阻力 `11.13`。回踩小仓试探，接近阻力减仓。
- `002074.SZ`：`OFI_z` 转强但 `VWAP_z` 仍偏负，支撑 `37.73`、阻力 `40.57`。仅在支撑附近小仓试探，不追高。

### 仅做反弹卖出端
- `300771.SZ`：`OFI_z` 转负，`VWAP_z` 转负，支撑 `14.31`、阻力 `15.19`。反弹减仓，回落再看。
- `600201.SH`：从强转弱，且密集区仅剩阻力。暂不加T，反弹减仓为主。

### 暂不建议买入做T（持有观察/反弹减仓）
- `003040.SZ`：`VWAP_z` 明显转负，密集区仅阻力 `18.14`，不适合加T。
- `600693.SH`：`OFI_z` 仍弱、`VWAP_z` 偏负，密集区阻力 `12.92`，等待修复。
- `601933.SH`：`queue_pressure` 明显偏弱，密集区阻力 `4.52`，不加T。

## 风险提示
- A股为 `T+1`，以上建议均基于已有底仓进行滚动，不适用于当天新买当天卖。
- 若明日早盘出现量价背离（价升量弱或量升价弱），优先执行“反弹减仓”策略。
