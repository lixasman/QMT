- [x] BVC：lookback=100、min_periods=20、σ<1e-8 时 v_buy=v_sell=V/2
- [x] VPIN：bucket_size=total_volume/n_buckets，末桶丢弃，window=50 的滚动 VPIN 序列正确
- [x] VPIN：premium_rate 可用时启用溢折价过滤，阈值/饱和值与策略一致
- [x] OFI：Cont-Stoikov 分段定义与聚合（daily/20/60/240）一致，缺失列时退化为不可用
- [x] Kyle's Lambda：使用末尾 240 快照回归口径一致，样本不足/方差过小返回 nan
- [x] Volume Surprise：按 20 快照=1分钟聚合后计算 VS(t)=V/EMA(V,20)，输出 vs_mean/vs_max/vs_last
- [x] FeaturePipeline：VPIN 用 rolling rank，OFI/Lambda 用 rolling zscore，VS 用 log+zscore，且仅使用最近 history_days 窗口
- [x] MicrostructureEngine：raw/features 输出字段命名与策略文档约定一致或有明确映射表
- [x] 日批合并：premium_vpin_cross / profit_ofi_divergence / asr_velocity 计算与策略一致并出现在输出中
- [x] 回归测试：最小集合通过，覆盖关键公式与退化路径
- [x] 最终交付：输出核对结果摘要（差异点、修改点、验证方式与结论）
