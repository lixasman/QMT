- [x] 仅新增集成层文件与测试，未修改 core/entry/exit/position/t0 现有文件
- [x] `StrategyConfig` 默认值与 CLI 覆盖行为符合 spec（至少 adapter、broker 可覆盖）
- [x] `main.py` 可作为唯一入口启动并输出阶段切换日志
- [x] `StrategyRunner` 初始化确保 FSM/StateManager/DataAdapter/TradingAdapter 共享同一实例
- [x] 启动时自动创建 `data/state/` 与 `data/logs/` 目录
- [x] pre_open 执行：同步资产、执行 pending_locked、重置 `t0_daily_pnl`、调用 `reset_t0_daily()` 与 `t0_prepare_day()`、更新 watchlist
- [x] intraday tick 周期对每个 ETF 先做数据质量检查，不合格则跳过所有动作
- [x] Entry 下单后可轮询 `confirm_order()` 并正确回调 PositionFSM（trial/confirm/failed）且释放锁资
- [x] Exit 下单后可确认成交量并回调 PositionFSM（layer1_clear/layer2_reduce）以补全数量清零/拆分更新
- [x] tick 周期对单 ETF/单模块异常可捕获并记录，不影响其他 ETF 继续执行
- [x] 关键状态变更后调用 `StateManager.save()`，日终状态文件存在且可反序列化
- [x] post_close 追加 `t0_daily_pnl` 到 5/30 日窗口并截断长度，调用 `on_post_close(now, current_nav)`
- [x] GUI 模式触发操作限额冻结后，post_close 调用 `exit_freeze_mode()` 完成重置
- [x] 集成测试文件存在且覆盖：日循环、全流程联动、数据降级跳过、GUI 限额
- [x] README 新增“模块 4：策略交易系统”章节并包含两种模式启动示例
